# Dependencies shared with the CPU image. torch and llama-cpp-python are
# installed explicitly in Dockerfile.gpu so their CUDA build cannot be
# replaced by a CPU wheel during dependency resolution.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.9.2
sentence-transformers==3.2.1
huggingface_hub==0.26.2
