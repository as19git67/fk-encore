version: "3.9"

services:
  insightface:
    build: .
    container_name: insightface-service
    # Loopback only — the service has no authentication of its own.
    ports:
      - "127.0.0.1:8002:8000"
    restart: unless-stopped
