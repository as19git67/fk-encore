services:
  landmark-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: landmark-service
    restart: unless-stopped
    environment:
      LANDMARK_THRESHOLD: "0.35"
      LOG_LEVEL: INFO
    ports:
      - "8003:8000"
