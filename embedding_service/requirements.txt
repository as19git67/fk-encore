# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Settings
pydantic-settings==2.3.0

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.31
pgvector==0.3.2

--extra-index-url https://download.pytorch.org/whl/cpu

# ML – install CPU torch by default; override with GPU wheel in Dockerfile if needed
torch==2.10.0+cpu
torchvision==0.25.0+cpu

# OpenCLIP
open-clip-torch==2.26.1

# DINOv2 via HuggingFace Transformers
transformers==4.43.3
accelerate==0.32.0

# Image processing
Pillow==10.4.0
pillow-heif==0.18.0

# Utilities
numpy==1.26.4

# NLP – natural language query parsing (German NER + relative date parsing)
spacy==3.7.5
dateparser==1.2.0

# ONNX export + INT8 quantisation. Used by app/scripts/export_onnx.py
# (build/runtime tool) and OnnxInt8Backend (production inference path).
# onnxruntime CPU build only; the GPU variant pulls in CUDA which we don't
# have on the production target. onnxscript is required by torch>=2.5's
# default Dynamo-based exporter — we currently force the legacy TorchScript
# path via dynamo=False, but keeping onnxscript here means a future torch
# upgrade that drops the legacy path doesn't break the export script.
onnx==1.17.0
onnxruntime==1.20.1
onnxscript==0.1.0
